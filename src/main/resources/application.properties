spring.application.name=hotel_service

spring.datasource.url=jdbc:mysql://localhost:3306/hotel_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC

spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:1122334455}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.generate-ddl=true
spring.sql.init.platform=all
spring.jpa.open-in-view=false

server.port=9000
#key-token
jwt.signerKey=${JWT_SIGNER_KEY:"1TjXchw5FloESb63Kc+DFhTARvpWL4jUGCwfGWxuG5SIf/1y/LgJxHnMqaF6A/ij"}
jwt.accessTokenMinutes=60
jwt.refreshTokenDays=7


# Bootstrap admin is disabled by default for safer deployments.
app.bootstrap.admin.enabled=${BOOTSTRAP_ADMIN_ENABLED:false}
app.bootstrap.admin.username=${BOOTSTRAP_ADMIN_USERNAME:admin}
app.bootstrap.admin.email=${BOOTSTRAP_ADMIN_EMAIL:admin@hotel.local}
app.bootstrap.admin.password=${BOOTSTRAP_ADMIN_PASSWORD:admin}

