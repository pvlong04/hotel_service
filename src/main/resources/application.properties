spring.application.name=hotel_service
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/hotel_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:1122334455}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.generate-ddl=false
spring.sql.init.platform=all
spring.jpa.open-in-view=false
server.port=${PORT:8080}
server.forward-headers-strategy=framework
app.cors.allowed-origin-patterns=${APP_CORS_ALLOWED_ORIGIN_PATTERNS:http://localhost:3000,http://localhost:5173,https://*.ngrok-free.dev,https://*.onrender.com,https://*.vercel.app,https://*.netlify.app}
# Flyway
# Existing DB already has objects, so baseline first to avoid startup failure.
spring.flyway.locations=classpath:migrationDB
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=1.0.2
spring.flyway.schemas=hotel_db
#key-token
#jwt.signerKey=${JWT_SIGNER_KEY:${JWT_SECRET}}
jwt.signerKey=${JWT_SIGNER_KEY:"1TjXchw5FloESb63Kc+DFhTARvpWL4jUGCwfGWxuG5SIf/1y/LgJxHnMqaF6A/ij"}
jwt.accessTokenMinutes=60
jwt.refreshTokenDays=7
# Bootstrap admin account for local development.
app.bootstrap.admin.enabled=${BOOTSTRAP_ADMIN_ENABLED:true}
app.bootstrap.admin.username=${BOOTSTRAP_ADMIN_USERNAME:admin}
app.bootstrap.admin.email=${BOOTSTRAP_ADMIN_EMAIL:admin@hotel.local}
app.bootstrap.admin.password=${BOOTSTRAP_ADMIN_PASSWORD:admin123}
# VNPay (sandbox)
vnpay.tmnCode=${VNPAY_TMN_CODE:IM4QDXT3}
vnpay.hashSecret=${VNPAY_HASH_SECRET:EPRS2Q12ZA57F8NR4K4GRI9U13QWSL06}
vnpay.payUrl=${VNPAY_PAY_URL: https://sandbox.vnpayment.vn/paymentv2/vpcpay.html}
vnpay.returnUrl=${VNPAY_RETURN_URL:http://localhost:3000/payment/vnpay-return}
vnpay.version=${VNPAY_VERSION:2.1.0}
vnpay.command=${VNPAY_COMMAND:pay}
vnpay.currCode=${VNPAY_CURR_CODE:VND}
vnpay.locale=${VNPAY_LOCALE:vn}
vnpay.orderType=${VNPAY_ORDER_TYPE:other}
vnpay.bankCode=${VNPAY_BANK_CODE:VNPAYQR}
# Image storage (local default)
app.images.upload-dir=${IMAGE_UPLOAD_DIR:uploads/images}
app.images.base-url=${IMAGE_BASE_URL:http://localhost:9000/images}
# Mail
app.mail.enabled=${APP_MAIL_ENABLED:true}
app.mail.from=${APP_MAIL_FROM:${MAIL_USERNAME:longthuong36294@gmail.com}}
app.mail.verify-base-url=${APP_MAIL_VERIFY_BASE_URL:${VERIFY_BASE_URL:http://localhost:3000/verify-email}}
app.mail.verify-token-ttl-hours=${APP_MAIL_VERIFY_TOKEN_TTL_HOURS:24}
app.mail.verify-resend-cooldown-seconds=${APP_MAIL_VERIFY_RESEND_COOLDOWN_SECONDS:60}
# Reset password
app.mail.reset-base-url=${APP_MAIL_RESET_BASE_URL:${RESET_BASE_URL:http://localhost:3000/reset-password}}
app.mail.reset-token-ttl-minutes=${APP_MAIL_RESET_TOKEN_TTL_MINUTES:30}
app.mail.reset-resend-cooldown-seconds=${APP_MAIL_RESET_RESEND_COOLDOWN_SECONDS:60}
# setting email
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:longthuong36294@gmail.com}
spring.mail.password=${MAIL_PASSWORD:lhlk sido mupn bxol}
spring.mail.properties.mail.smtp.auth=${MAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${MAIL_SMTP_STARTTLS:true}
spring.mail.properties.mail.smtp.starttls.required=${MAIL_SMTP_STARTTLS_REQUIRED:true}
# AI - Ollama (local LLM)
spring.ai.ollama.base-url=${OLLAMA_BASE_URL:http://localhost:11434}
spring.ai.ollama.chat.options.model=${OLLAMA_CHAT_MODEL:gemma3:1b}
spring.ai.ollama.chat.options.temperature=0.7
# Disable embedding auto-configuration because this project currently uses chat only.
spring.ai.ollama.embedding.enabled=true
# Do not pull models during Spring startup to avoid boot failure when Ollama is slow/offline.
spring.ai.ollama.init.pull-model-strategy=never
spring.ai.ollama.init.timeout=120s
# VNPay reconciliation: auto-fail pending payments and cancel reservation when timeout
app.payment.vnpay.pending-timeout-minutes=${APP_PAYMENT_VNPAY_PENDING_TIMEOUT_MINUTES:16}
app.payment.vnpay.reconcile-delay-ms=${APP_PAYMENT_VNPAY_RECONCILE_DELAY_MS:60000}
